# 复制为 .env 并填写（本地开发前端）。
# 生产部署时在 CI / 构建环境中设置同名变量。

# 调用 Worker AI 端点（/api/chat、/api/speech、/api/realtime）所需的客户端令牌。
# 必须与 Worker 侧 `npx wrangler secret put CLIENT_ACCESS_TOKEN` 的值一致；
# 留空则 Worker 侧不启用令牌校验（仅限本地开发）。
VITE_CLIENT_ACCESS_TOKEN=
